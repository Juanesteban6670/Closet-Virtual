# Architecture

The backend uses layered boundaries: routes handle transport, services hold use cases, repositories isolate persistence, and models define database mappings. The frontend keeps routing, API clients, contexts, reusable UI, and pages separate.
